apiVersion: v1
kind: Service
metadata:
  name: {{ include "mern-chart.fullname" . }}
  labels:
    {{- include "mern-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mern-chart.selectorLabels" . | nindent 4 }}
